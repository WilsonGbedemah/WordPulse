version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Nothing to build - static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []