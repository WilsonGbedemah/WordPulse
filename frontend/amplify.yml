version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build needed for static site"
  artifacts:
    baseDirectory: /src       # Points to your files
    files:
      - '**/*'                # Include all files
  cache:
    paths: []