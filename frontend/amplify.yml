version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies"
    build:
      commands:
        - echo "No build needed"
  artifacts:
    baseDirectory: /src    # Critical! Must match your folder
    files:
      - '**/*'             # Include all files
  cache:
    paths: []