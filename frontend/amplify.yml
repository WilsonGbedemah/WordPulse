version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static site"
  artifacts:
    baseDirectory: /src   # 👈 Key change: Look in /src instead of root
    files:
      - '**/*'           # Include all files in /src
  cache:
    paths: []